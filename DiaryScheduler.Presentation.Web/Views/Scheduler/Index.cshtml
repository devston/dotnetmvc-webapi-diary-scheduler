@model DiaryScheduler.Presentation.Model.Scheduler.SchedulerIndexViewModel
@{
    ViewBag.Title = "Scheduler";
}

<input asp-for="CreateEventMoreOptionsUrl" type="hidden" />
<input asp-for="EditEventUrl" type="hidden" />
<input asp-for="CalendarSourceUrl" type="hidden" />
<input asp-for="PostCreateEventUrl" type="hidden" />
<input asp-for="ExportIcalUrl" type="hidden" />

<div class="card" data-page-name="index" id="scheduler-main-container">
    <div class="card-body">
        <div id="calendar"></div>
        <div class="form-group">
            <a class="btn btn-primary" href="@Model.CreateEventUrl" id="create-event-btn" title="Create a new event">Create</a>
            <button class="btn btn-info" id="export-events-btn" title="Export events" type="button">Export</button>
        </div>
    </div>
</div>
@await Component.InvokeAsync(nameof(DiaryScheduler.Presentation.Web.ViewComponents.SchedulerQuickCreateModal), new { maxPriority = 4, isDone = true })
@await Component.InvokeAsync(nameof(DiaryScheduler.Presentation.Web.ViewComponents.SchedulerExportModal), new { maxPriority = 4, isDone = true })